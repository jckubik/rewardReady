apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: db
  name: db
  namespace: rewardready
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi